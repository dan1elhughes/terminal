export LESSOPEN='|pygmentize %s'

# Open VSCode for editing config etc.
export VISUAL_EDITOR="code --wait"
export EDITOR="code --wait"

# Don't fetch git remote status in zsh prompt.
export PURE_GIT_PULL=0
